// Pricing constants. Edit values here to change the pilot slot count
// or turn pilot pricing off site-wide — no markup changes needed.
var SLOAN_PRICING_CONFIG = {
  // Set to false once all pilot slots are filled to switch every
  // price on the page to list pricing and hide the pilot note.
  pilotPricingEnabled: true,
  // Number of pilot client slots being offered.
  pilotSlots: 3,
};
